[package]
name = "voxlog-input-output"
version = "0.1.0"
edition = "2021"
description = "input/output policy helpers for VoxLog desktop"

[dependencies]
serde = { version = "1", features = ["derive"] }
