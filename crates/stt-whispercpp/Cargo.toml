[package]
name = "voxlog-stt-whispercpp"
version = "0.1.0"
edition = "2021"
description = "whisper.cpp adapter scaffolding for VoxLog desktop"

[dependencies]
serde = { version = "1", features = ["derive"] }
